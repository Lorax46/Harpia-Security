# docker-compose.yml (local dev)
version: '3.8'

services:
  harpia:
    build: .
    ports:
      - "9090:9090"
    environment:
      - TZ=UTC
    volumes:
      - .:/app
    restart: unless-stopped
